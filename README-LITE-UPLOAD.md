# Versi Lite Upload GitHub Pages

Folder ini adalah full raw ringan untuk `bahanajaronline-foodbaveragescomplete`.

Perubahan ukuran:
- `worksheet-online.pdf` diganti versi compressed.
- Folder `assets/` dihapus karena gambar yang dipakai HTML sudah tersedia di root folder.
- Dropdown Cabang dan Nama Teacher sudah masuk di `test-config.js`.

Upload isi folder ini ke root repo GitHub Pages. Jika GitHub menanyakan replace file lama, pilih replace/overwrite.

Setelah deploy, cek:
- `test-config.js?v=dropdown-lite` harus menampilkan `Mathpreneur ONLINE`.
- `pre-test-day1.html?v=dropdown-lite` dropdown Cabang dan Nama Teacher harus sudah panjang.
